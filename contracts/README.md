# Laundry platform contracts

Versioned HTTP payload and event types shared by the portals and services. When
repositories are split, publish this package to the organization registry and
pin an exact compatible version in each consumer.
