# Sales operations upgrade

## Confirmed scope

- One shop-wide daily session with opening cash, expected cash, counted closing cash and a closing difference.
- Hotel deliveries stay separate from walk-in orders and each receive an individual A4 invoice.
- Keep the browser print dialog. Save and print must not add an application confirmation screen.
- Per-staff operation permissions and configurable history windows are enforced by the API.
- Quantity and invoice-only rate overrides never change the saved price list.

Implementation and verification notes will be updated alongside the code.
